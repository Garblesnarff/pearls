# Server Configuration
PORT=8889
NODE_ENV=development
BASE_URL=http://localhost:8889

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/pearls

# WorkOS Authentication
# Get these from https://dashboard.workos.com
WORKOS_API_KEY=sk_test_xxxxx
WORKOS_CLIENT_ID=client_xxxxx

# JWT Configuration
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here

# Role Configuration (comma-separated WorkOS user IDs)
# Users with admin role can create threads and have full access
ADMIN_USER_IDS=user_xxxxx

# Users with aurora:member role (for private thread access)
AURORA_MEMBER_IDS=user_xxxxx

# OpenAI API Key (for vector search embeddings)
# Optional - if not set, vector search will be disabled
OPENAI_API_KEY=sk-xxxxx
